{
  "name": "qa-api-supertest",
  "version": "1.0.0",
  "private": true,
  "description": "Node.js API test framework using Supertest, Jest, Express, and consumer contract verification.",
  "main": "src/server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=22 <23 || >=24 <25"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "jest --runInBand",
    "test:coverage": "jest --runInBand --coverage",
    "test:contract": "jest src/contracts --runInBand",
    "test:api": "jest src/tests --runInBand",
    "test:live-smoke": "node scripts/live-smoke.js",
    "workflow-pins:check": "node scripts/validate-workflow-pins.js",
    "check": "node --check src/app.js && node --check src/server.js && node --check src/config.js && node --check src/correlation.js && node --check src/urlPolicy.js && node --check src/clients/postsUpstreamClient.js && node --check src/clients/upstreamError.js && node --check src/middleware/requestContext.js && node --check src/routes/posts.js && node --check src/testing/apiAgent.js && node --check src/testing/expectations.js && node --check scripts/live-smoke.js && node --check scripts/validate-ci-evidence.js && node --check scripts/validate-listener-evidence.js && node --check scripts/validate-workflow-pins.js && npm run workflow-pins:check"
  },
  "license": "MIT",
  "dependencies": {
    "@pact-foundation/pact": "^17.1.2",
    "axios": "^1.6.7",
    "express": "^5.2.1",
    "supertest": "^7.2.2"
  },
  "devDependencies": {
    "jest": "^30.4.2",
    "nodemon": "^3.0.1"
  },
  "packageManager": "npm@11.19.1"
}
